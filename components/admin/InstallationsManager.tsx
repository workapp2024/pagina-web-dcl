/* eslint-disable @typescript-eslint/no-explicit-any */
"use client";
import {useEffect,useMemo,useState} from "react";
export function InstallationsManager(){const[rows,setRows]=useState<any[]>([]),[q,setQ]=useState(''),[status,setStatus]=useState('pending'),[error,setError]=useState('');const load=()=>fetch('/api/admin/installations').then(r=>r.json()).then(x=>x.ok?setRows(x.data||[]):setError(x.error));useEffect(()=>{void load()},[]);const list=useMemo(()=>rows.filter(x=>(status==='all'||x.status===status)&&JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[rows,q,status]);async function save(x:any,next=x.status){const r=await fetch('/api/admin/installations',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id:x.id,status:next,scheduledAt:x.scheduled_at,notes:x.notes,location:x.location,contactPhone:x.contact_phone,workType:x.work_type,difficulty:x.estimated_difficulty,technician:x.assigned_technician})});if(!r.ok)setError('No se pudo actualizar la instalación.');else void load()}const field="rounded-xl border border-white/10 bg-zinc-900 p-3 text-sm";return <div className="space-y-5"><header><h1 className="text-3xl font-black">Instalaciones</h1><p className="mt-2 text-sm text-zinc-400">Agenda vinculada a cliente, venta, vehículo y productos.</p></header><div className="flex flex-col gap-3 sm:flex-row"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cliente, vehículo, producto o teléfono" className={`flex-1 ${field}`}/><select value={status} onChange={e=>setStatus(e.target.value)} className={field}><option value="pending">Pendientes</option><option value="completed">Realizadas</option><option value="all">Todas</option></select></div>{error&&<p className="text-red-300">{error}</p>}<div className="space-y-3">{list.map(x=><article key={x.id} className="rounded-2xl border border-white/10 p-4"><div className="flex flex-wrap justify-between gap-3"><span><b>{x.sales?.customers?.full_name||'Cliente'}</b><p className="text-sm text-zinc-400">{x.customer_vehicles?`${x.customer_vehicles.brand_name} ${x.customer_vehicles.model_name} ${x.customer_vehicles.plate||''}`:'Sin vehículo'} · {x.sales?.sale_items?.map((i:any)=>`${i.product_name} × ${i.quantity}`).join(', ')}</p></span><b className="text-sm text-red-300">{x.status}</b></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><input type="datetime-local" value={x.scheduled_at?.slice(0,16)||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,scheduled_at:e.target.value}:r))} className={field}/><input value={x.contact_phone||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,contact_phone:e.target.value}:r))} placeholder="Teléfono" className={field}/><input value={x.location||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,location:e.target.value}:r))} placeholder="Ubicación" className={field}/><input value={x.work_type||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,work_type:e.target.value}:r))} placeholder="Tipo de trabajo" className={field}/><select value={x.estimated_difficulty||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,estimated_difficulty:e.target.value}:r))} className={field}><option value="">Dificultad sin definir</option><option value="low">Baja</option><option value="medium">Media</option><option value="high">Alta</option></select><input value={x.assigned_technician||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,assigned_technician:e.target.value}:r))} placeholder="Técnico" className={field}/><textarea value={x.notes||''} onChange={e=>setRows(all=>all.map(r=>r.id===x.id?{...r,notes:e.target.value}:r))} placeholder="Observaciones" className={`${field} sm:col-span-2 lg:col-span-3`}/></div><div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>void save(x)} className="rounded-full border border-white/20 px-4 py-2 text-xs">Guardar cambios</button>{x.status!=='completed'&&<button onClick={()=>void save(x,'completed')} className="rounded-full bg-red-600 px-4 py-2 text-xs">Marcar realizada</button>}</div></article>)}</div></div>}
