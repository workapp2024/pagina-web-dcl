/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { useSiteContent } from "@/components/providers/SiteContentProvider";
import { ManagedImage } from "@/components/ui/ManagedImage";
import { WhatsAppButton } from "@/components/ui/WhatsAppButton";
import { analyticsEvents, capture } from "@/lib/analytics";
import { getPublicVehicleBrands,getPublicVehicleModels,getPublicVehicleTypes,searchPublicVehicleCompatibilities,VEHICLE_TYPES,type VehicleBrand,type VehicleCompatibilityFull,type VehicleModel } from "@/lib/supabase/vehicle-compatibility";
import type { Product } from "@/lib/site-data";

const connectorsFor=(row:VehicleCompatibilityFull)=>Array.from(new Set([row.connectorLow,row.combinedHighLow?null:row.connectorHigh,row.connectorFog,row.connectorAux].filter((x):x is string=>Boolean(x))));
const range=(row:VehicleCompatibilityFull)=>row.yearTo?`${row.yearFrom}–${row.yearTo}`:`Desde ${row.yearFrom}`;
function Products({products,connectors}:{products:Product[];connectors:string[]}){const values=new Set(connectors.map(x=>x.toLowerCase()));const matches=products.filter(p=>p.active&&p.showInCatalog&&p.connectorType&&values.has(p.connectorType.toLowerCase()));if(!matches.length)return null;return <div><h3 className="mb-3 text-lg font-black uppercase">Productos compatibles</h3><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{matches.map(p=><Link key={p.id} href={p.href} className="flex gap-3 rounded-2xl border border-white/10 bg-white/5 p-3"><div className="flex h-20 w-20 shrink-0 items-center justify-center rounded-xl bg-black p-2"><ManagedImage source={p.image} alt={p.name} className="max-h-full max-w-full object-contain"/></div><div><b>{p.name}</b><small className="mt-1 block text-zinc-400">{p.category}</small><span className="mt-2 block text-xs font-bold text-red-300">Ver producto →</span></div></Link>)}</div></div>}
export function VehicleFinder(){const{content}=useSiteContent();const[types,setTypes]=useState<string[]>([...VEHICLE_TYPES]),[type,setType]=useState(""),[brands,setBrands]=useState<VehicleBrand[]>([]),[brand,setBrand]=useState(""),[models,setModels]=useState<VehicleModel[]>([]),[model,setModel]=useState(""),[year,setYear]=useState(""),[years,setYears]=useState<number[]>([]),[results,setResults]=useState<VehicleCompatibilityFull[]|null>(null),[searching,setSearching]=useState(false);useEffect(()=>{getPublicVehicleTypes().then(x=>x?.length&&setTypes(x))},[]);useEffect(()=>{setBrand("");setModel("");setYear("");setBrands([]);setModels([]);setYears([]);setResults(null);if(type)getPublicVehicleBrands(type).then(x=>setBrands(x||[]))},[type]);useEffect(()=>{setModel("");setYear("");setModels([]);setYears([]);setResults(null);if(brand)getPublicVehicleModels(brand,type).then(x=>setModels(x||[]))},[brand,type]);useEffect(()=>{setYear("");setYears([]);if(model)searchPublicVehicleCompatibilities(type,brand,model).then(rows=>{const now=new Date().getFullYear()+1,set=new Set<number>();(rows||[]).forEach(r=>{for(let y=r.yearFrom;y<=Math.min(r.yearTo||now,now);y++)set.add(y)});setYears([...set].sort((a,b)=>b-a))})},[model,type,brand]);async function search(){if(!type)return;setSearching(true);capture(analyticsEvents.vehicleSearchStarted);const rows=await searchPublicVehicleCompatibilities(type,brand||undefined,model||undefined),y=year?Number(year):null,filtered=(rows||[]).filter(r=>y===null||(y>=r.yearFrom&&(r.yearTo===null||y<=r.yearTo)));setResults(filtered);setSearching(false);const props={vehicle_type:type,brand:brands.find(x=>x.id===brand)?.name||"",model:models.find(x=>x.id===model)?.name||"",year_provided:Boolean(year),result_count:filtered.length};capture(filtered.length?analyticsEvents.vehicleSearchCompleted:analyticsEvents.vehicleSearchNoResults,props)}const connectors=useMemo(()=>Array.from(new Set((results||[]).flatMap(connectorsFor))),[results]);return <div className="space-y-5"><div className="rounded-[1.75rem] border border-white/10 bg-zinc-950/60 p-5 sm:p-6"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Select label="Tipo de vehículo" value={type} onChange={setType} options={types.map(x=>[x,x])}/><Select label="Marca (opcional)" value={brand} onChange={setBrand} disabled={!type} options={brands.map(x=>[x.id,x.name])}/><Select label="Modelo (opcional)" value={model} onChange={setModel} disabled={!brand} options={models.map(x=>[x.id,x.name])}/><Select label="Año (opcional)" value={year} onChange={setYear} disabled={!model||!years.length} options={years.map(x=>[String(x),String(x)])}/></div><button type="button" disabled={!type||searching} onClick={search} className="mt-5 min-h-12 rounded-full bg-red-600 px-8 text-xs font-bold uppercase text-white disabled:opacity-40">{searching?"Buscando…":"Buscar"}</button>{results&&<div className="mt-7 space-y-5"><div><h3 className="text-lg font-black uppercase">Compatibilidades encontradas</h3><p className="text-sm text-zinc-400">{results.length} resultado(s). Completá más campos para refinar.</p></div>{results.map(r=><article key={r.id} className="rounded-2xl border border-white/10 bg-white/5 p-4"><b>{r.vehicleType} · {r.brandName} {r.modelName}</b><p className="text-sm text-zinc-400">{range(r)}{r.version?` · ${r.version}`:""}</p><p className="mt-2 text-xs text-red-200">{connectorsFor(r).join(" · ")}</p></article>)}<Products products={content.products} connectors={connectors}/></div>}</div><div className="rounded-2xl border border-white/10 p-5 text-center"><p className="font-bold">¿No encontraste tu vehículo?</p><WhatsAppButton source="vehicle_search" label="Consultar por WhatsApp" className="mt-4 min-h-12"/></div></div>}
function Select({label,value,onChange,options,disabled}:{label:string;value:string;onChange:(v:string)=>void;options:string[][];disabled?:boolean}){return <label><span className="mb-2 block text-[10px] font-bold uppercase tracking-widest text-zinc-400">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} disabled={disabled} className="min-h-12 w-full rounded-xl border border-white/10 bg-zinc-900 px-3 text-white disabled:opacity-40"><option value="">Cualquiera</option>{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>}
