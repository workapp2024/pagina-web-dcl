"use client";
import {useMemo,useState} from "react";
import type {Product} from "@/lib/site-data";
import {ProductCard} from "@/components/ui/ProductCard";
export function ProductCatalog({products}:{products:Product[]}){const[query,setQuery]=useState(''),[category,setCategory]=useState('all');const categories=useMemo(()=>[...new Set(products.map(p=>p.category))].sort(),[products]);const visible=useMemo(()=>{const needle=query.trim().toLowerCase();return products.filter(p=>(category==='all'||p.category===category)&&(!needle||[p.name,p.description,p.category,p.connectorType].some(v=>String(v||'').toLowerCase().includes(needle))))},[products,query,category]);return <><div className="mb-7 grid gap-3 sm:grid-cols-[1fr_220px]"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar producto, categoría o conector" className="min-h-12 rounded-xl border border-white/10 bg-zinc-950 px-4 text-white"/><select value={category} onChange={e=>setCategory(e.target.value)} className="min-h-12 rounded-xl border border-white/10 bg-zinc-950 px-4 text-white"><option value="all">Todas las categorías</option>{categories.map(value=><option key={value}>{value}</option>)}</select></div>{visible.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{visible.map(product=><ProductCard key={product.id}{...product}/>)}</div>:<p className="rounded-2xl border border-white/10 bg-white/5 p-6 text-sm text-zinc-400">No encontramos productos para esa búsqueda.</p>}</>}
