import type {GalleryItem} from "@/lib/supabase/gallery";import {ManagedImage} from "@/components/ui/ManagedImage";
export function WorkGallery({items}:{items:GalleryItem[]}){if(!items.length)return null;const sequence=(copy:string,hidden=false)=><div className="gallery-sequence flex shrink-0 gap-4" aria-hidden={hidden||undefined}>{items.map(item=><article key={`${copy}-${item.id}`} className="relative aspect-[4/3] w-[62vw] max-w-sm shrink-0 overflow-hidden rounded-3xl border border-white/10 bg-zinc-900 sm:w-[38vw] lg:w-[18vw]"><ManagedImage source={item.imageUrl} alt={hidden?"":item.title||"Trabajo realizado por DCL"} loading="lazy" decoding="async" className="h-full w-full object-cover"/><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black via-black/70 to-transparent p-4 pt-12">{item.title&&<h3 className="font-bold text-white">{item.title}</h3>}{item.caption&&<p className="mt-1 text-sm text-zinc-300">{item.caption}</p>}</div></article>)}</div>;return <section className="border-y border-white/10 bg-zinc-950 py-14" aria-labelledby="gallery-title"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><span className="text-xs font-bold uppercase tracking-[.24em] text-red-400">Experiencia DCL</span><h2 id="gallery-title" className="mt-2 text-3xl font-black uppercase tracking-[-.05em]">DCL en acción</h2><p className="mt-3 max-w-2xl text-sm text-zinc-400">Entregas e instalaciones reales que muestran cómo queda cada solución.</p></div><div className="gallery-marquee mt-7 overflow-hidden"><div className="gallery-track flex w-max gap-4 will-change-transform">{sequence("a")}{sequence("b",true)}</div></div></section>}
